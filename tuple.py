# is an ordered  sequencxe of items, same as a list
#  different between tuple and list is that tuple are immutable
# we use () to create a tuple eg product=("xbox", 499.99, "playlist")
# Allows dublicates
product = ('Microsoft', 'Xbox', 499.99)

#  accesselement at index 0
print(product[0])
print(product[2])


# create a tuple to store 4 programming languages and print  is out
languages = ("Python", "Java", "SQL", "C#")
print(languages)

# Find the lenght of the tuple
print(len(languages))
# Checking the data type of the tuple
print(type(languages))